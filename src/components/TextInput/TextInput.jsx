import React from 'react';
import PropTypes from 'prop-types';
import styles from './TextInput.scss';

const TextInput = () => (
    <input type="text" className={styles.general} />
);

export default TextInput;

